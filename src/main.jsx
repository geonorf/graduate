import React from 'react'
import ReactDOM from 'react-dom/client'
import WebControl from './WebControl.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <WebControl />
  </React.StrictMode>,
)
